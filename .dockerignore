# =====================================================
# .dockerignore — Docker 构建上下文排除清单
# 本镜像 Dockerfile 只 COPY docker-entrypoint.sh，
# 其余文件不参与构建；排除后减小上下文、加速打包
# =====================================================

# 版本控制（.git 历史体积大）
.git
.github

# macOS 系统文件（Finder 会自动生成）
.DS_Store

# 密钥 / 环境变量（安全红线：防止泄露进构建上下文）
.env
.env.*

# 文档（不参与构建）
README.md
